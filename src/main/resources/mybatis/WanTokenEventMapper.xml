<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cdkj.coin.dao.IWanTokenEventDAO">
	<resultMap id="wanTokenEvent_DB_Result" type="wanTokenEvent">
		<result column="id" property="id" />
		<result column="hash" property="hash" />
		<result column="token_from" property="tokenFrom" />
		<result column="token_to" property="tokenTo" />
		<result column="token_value" property="tokenValue" />
		<result column="token_log_index" property="tokenLogIndex" />
		<result column="symbol" property="symbol" />
	</resultMap>
	<sql id="where_condition">
		<trim prefix="WHERE" prefixOverrides="AND | OR">
			<if test="hash != null and hash != '' ">
				AND t.hash = #{hash}
			</if>
			<if test="tokenFrom != null and tokenFrom != '' ">
				AND t.token_from = #{tokenFrom}
			</if>
			<if test="tokenTo != null and tokenTo != '' ">
				AND t.token_to = #{tokenTo}
			</if>
			<if test="symbol != null and symbol != '' ">
				AND t.symbol = #{symbol}
			</if>
		</trim>
	</sql>
	<select id="select_wanTokenEvent" parameterType="wanTokenEvent" resultMap="wanTokenEvent_DB_Result">
		SELECT
		t.id
		,t.hash
		,t.token_from
		,t.token_to
		,t.token_value
		,t.token_log_index
		,t.symbol
		FROM
		tctq_eth_token_event t
		<include refid="where_condition" />
		<trim prefix="ORDER BY ">
			<if test="order != null and order != '' ">
				t.${order}
			</if>
		</trim>
	</select>

	<select id="select_wanTokenEvent_count" parameterType="wanTokenEvent"
		resultType="java.lang.Long">
		SELECT count(1) FROM tctq_eth_token_event t
		<include refid="where_condition" />
	</select>

	<insert id="insert_wanTokenEvent" parameterType="wanTokenEvent">
		INSERT INTO
		tctq_eth_token_event(
		id
		,hash
		,token_from
		,token_to
		,token_value
		,token_log_index
		,symbol
		)
		VALUES(
		#{id}
		,#{hash}
		,#{tokenFrom}
		,#{tokenTo}
		,#{tokenValue}
		,#{tokenLogIndex}
		,#{symbol}
		)
	</insert>
	
	<insert id="insert_eventList" parameterType="java.util.List">
	
		INSERT INTO `tctq_eth_token_event`(
			`hash`
			,`token_from`
			,`token_to`
			,`token_value`
			,`token_log_index`
			
			,`symbol`
			)
		VALUES

		  <foreach collection="list" item="item" separator=",">
			  (
			  #{item.hash} 
			,#{item.tokenFrom} 
			,#{item.tokenTo} 
			,#{item.tokenValue}
			,#{item.tokenLogIndex}
			
			,#{item.symbol}
			  )
		  </foreach>
	</insert>

	<update id="update_wanTokenEvent" parameterType="wanTokenEvent">
		UPDATE tctq_eth_token_event
		SET
		hash = #{hash}
		,token_from = #{tokenFrom}
		,token_to = #{tokenTo}
		,token_value = #{tokenValue}
		,token_log_index = #{tokenLogIndex}
		,symbol = #{symbol}
		WHERE code = #{code}
	</update>


</mapper>