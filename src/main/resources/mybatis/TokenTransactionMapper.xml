<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cdkj.coin.dao.ITokenTransactionDAO">
	<resultMap id="tokenTransaction_DB_Result" type="tokenTransaction">
		<result column="id" property="id" />
		<result column="hash" property="hash" />
		<result column="nonce" property="nonce" />
		<result column="block_hash" property="blockHash" />
		<result column="transaction_index" property="transactionIndex" />
		<result column="from" property="from" />
		<result column="to" property="to" />
		<result column="value" property="value" />
		<result column="input" property="input" />
		<result column="token_from" property="tokenFrom" />
		<result column="token_to" property="tokenTo" />
		<result column="token_value" property="tokenValue" />
		<result column="token_log_index" property="tokenLogIndex" />
		<result column="block_create_datetime" property="blockCreateDatetime" />
		<result column="sync_datetime" property="syncDatetime" />
		<result column="block_number" property="blockNumber" />
		<result column="gas_price" property="gasPrice" />
		<result column="gas_limit" property="gasLimit" />
		<result column="gas_used" property="gasUsed" />
		<result column="status" property="status" />
		<result column="symbol" property="symbol" />
	</resultMap>
	<sql id="where_condition">
		<trim prefix="WHERE" prefixOverrides="AND | OR">
			<if test="id != null and id != '' ">
				AND t.id = #{id}
			</if>
			<if test="hash != null and hash != '' ">
				AND t.hash = #{hash}
			</if>
			<if test="from != null and from != '' ">
				AND t.from = #{from}
			</if>
			<if test="to != null and to != '' ">
				AND t.to = #{to}
			</if>
			<if test="tokenFrom != null and tokenFrom != '' ">
				AND t.token_from = #{tokenFrom}
			</if>
			<if test="tokenTo != null and tokenTo != '' ">
				AND t.token_to = #{tokenTo}
			</if>
			<if test="status != null and status != '' ">
				AND t.status = #{status}
			</if>
			<if test="symbol != null and symbol != '' ">
				AND t.symbol = #{symbol}
			</if>
		</trim>
	</sql>
	<select id="select_tokenTransaction" parameterType="tokenTransaction" resultMap="tokenTransaction_DB_Result">
		SELECT
		t.id
		,t.hash
		,t.nonce
		,t.block_hash
		,t.transaction_index
		,t.from
		,t.to
		,t.value
		,t.input
		,t.token_from
		,t.token_to
		,t.token_value
		,t.token_log_index
		,t.block_create_datetime
		,t.sync_datetime
		,t.block_number
		,t.gas_price
		,t.gas_limit
		,t.gas_used
		,t.status
		,t.symbol
		FROM
		tctq_token_transaction t
		<include refid="where_condition" />
		<trim prefix="ORDER BY ">
			<if test="order != null and order != '' ">
				t.${order}
			</if>
		</trim>
	</select>

	<select id="select_tokenTransaction_count" parameterType="tokenTransaction"
		resultType="java.lang.Long">
		SELECT count(1) FROM tctq_token_transaction t
		<include refid="where_condition" />
	</select>

	<insert id="insert_tokenTransaction" parameterType="tokenTransaction">
		INSERT INTO
		tctq_token_transaction(
		hash
		,nonce
		,block_hash
		,transaction_index
		,`from`
		,`to`
		,value
		,input
		,token_from
		,token_to
		,token_value
		,token_log_index
		,block_create_datetime
		,sync_datetime
		,block_number
		,gas_price
		,gas_limit
		,gas_used
		,status
		,symbol
		)
		VALUES(
		#{hash}
		,#{nonce}
		,#{blockHash}
		,#{transactionIndex}
		,#{from}
		,#{to}
		,#{value}
		,#{input}
		,#{tokenFrom}
		,#{tokenTo}
		,#{tokenValue}
		,#{tokenLogIndex}
		,#{blockCreateDatetime}
		,#{syncDatetime}
		,#{blockNumber}
		,#{gasPrice}
		,#{gasLimit}
		,#{gasUsed}
		,#{status}
		,#{symbol}
		)
	</insert>

	<update id="update_status" parameterType="tokenTransaction">
		UPDATE tctq_token_transaction
		SET
		status = #{status}
		WHERE id = #{id}
	</update>


</mapper>